
export default function linkShell(editor) {
  let linkPlugin = editor.plugins.get('LinkUI')
  // console.log('linkShell', editor.plugins._plg, linkPlugin, linkPlugin._balloon)
  // linkPlugin.once('render', function () {
  //   console.log('render', this)
  // })
}
